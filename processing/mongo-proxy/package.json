{
  "name": "mukoko-mongo-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "MongoDB proxy Worker for Mukoko News Python Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:uri": "wrangler secret put MONGODB_URI"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
